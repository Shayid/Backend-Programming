const user = {
    name: "Mikhail Romanov",
    age: 27,
    address: "Kiev",
    isMarried: true,
};

const { name, age, address } = user;

console.log(name);
console.log(age);
console.log(address);